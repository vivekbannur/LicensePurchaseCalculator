# LicensePurchaseCalculator
This application calculates the minimum number of software license copies a company must purchase based on user device installations. The solution is designed to be extensible, testable, and scalable for large datasets.
